import '../stylesheets/CalculatorScreen.css';

const CalculatorScreen = ({ inputCalculator }) => (
    <div className='calculator-screen-input'>
        {inputCalculator}
    </div>
);

export default CalculatorScreen;